import { CreateProjectFormGroup } from './create-project-form-group';

describe('CreateProjectFormGroup', () => {
  it('should create an instance', () => {
    expect(new CreateProjectFormGroup()).toBeTruthy();
  });
});
